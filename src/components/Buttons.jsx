const Buttons = ({styles}) => {
  return (
    <button type='button' className={`py-4 px-6 bg-blue-gradient ${styles} rounded-md font-poppins font-medium text-[18px] text-primary`}>
      Get Started
    </button>
  )
}

export default Buttons